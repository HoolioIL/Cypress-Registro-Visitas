{
  "name": "first_code",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "tests": "cypress open",
    "edge-test": "cypress run --spec cypress/e2e/my-tests/register-visits.cy.js --browser edge",
    "chrome-test": "cypress run --spec cypress/e2e/my-tests/register-visits.cy.js --browser chrome"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cypress": "^10.3.0"
  }
}
